namespace exercicio{
    function calcularMultiplicacao(primeiroValor: number, segundoValor: number): number{
        primeiroValor = 10;
        segundoValor = 2;
        return primeiroValor * segundoValor;
    }
    
    function saudacao(nome:string): string{
        nome = "Marcelo";
        return "Olá " + nome;
    }

}